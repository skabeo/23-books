<div class='new-container'>
  <div class='sign-up-details'>
    <%= link_to authenticated_root_path do %>
      <%= image_tag 'back.png', class: 'back' %>
    <% end %>
    <h2 class='center-text'>Transactions</h2>
  </div>
  <div class='transaction-details'>
    <div class='trans-icon-name'>
      <p class='trans-icon'>
        <img src='<%= @category.icon %>' alt='category image' class='trans-cat-icon'></img>
      </p>
      <p class='trans-cat-name'><%= @category.name %></p>
    </div>

    <div class='trans-total'>
      <p class='bold-text'>Total Payment</p>
      <p>$ <%= @total_amount %></p>
    </div>
    
    <% if @details.empty? %>
      <p>Add a transaction</p>
    <% else %>
      <% @details.each do |detail| %>
        <div class='main-trans-details'>
          <p class='bold'><%= detail.name %></p>
          <p class='small-gap'><%= detail.created_at.strftime('%e %b %Y') %></p>
          <p class='small-gap'>$ <%= detail.amount %></p>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

<a href='/categories/<%= @category.id %>/details/new' class='add-item'>
  <div class='inner'>
    Add a transaction
  </div>
</a>
